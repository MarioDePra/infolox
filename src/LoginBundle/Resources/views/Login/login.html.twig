{% extends "html5_page_with_bootsrap_to_be_extended.html.twig" %}

{% block title %}
Bitte anmelden
{% endblock %}

{% block body %}
  <div class="container">
    <div class="panel panel-success">
      <div class="panel-heading">
        <h3>Login page</h3>
      </div>
      <div class="panel-body">
        {% if fehler %}
          <div class="alert alert-danger">
            {{ fehler.messageKey|trans(fehler.messageData, 'security') }}
          </div>
        {% endif %}
        <form action="{{ path('login') }}" method="post">
          <div class="form-group has-error">
            <label class="control-label" for="kennung">Kennung:</label>
            <input id="username" name="_username" class="form-control" type="email" aria-describedby="email_help" />
            <p id="email_help" class="help-block">Bitte gegben Sie ein E-Mail Adresse ein.</p>
          </div>
          <!-- div class="form-group">
            <select class="form-control" name="_kennung" id="kennung">
              <option value="North">North</option>
            </select>
          </div -->
          <div class="form-group">
            <label class="control-label" for="password">Passwort:</label>
            <input id="password" name="_password" class="form-control" type="password" />
          </div>
          <button class="btn btn-info pull-right">Anmeldung</button>
        </form>
      </div>
    </div>
  </div>
{% endblock %}


{% block javascripts %}
  $( function() {
    $("#username").autocomplete({
      source: [ {{ usernames|raw }} ]
    });
  } );
{% endblock %}
